# Todo CLI

A task manager web app with localStorage persistence.

## Run

```bash
docker compose up -d
# Open http://localhost:3001
```

## Features

- Add, complete, delete tasks
- Persistent via localStorage
- Filter: all, active, completed
- Dark theme